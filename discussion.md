讨论一下可行性，我可以有一个浏览器网页内的中文输入法吗？

这个网页监听按键，然后自带一个中文输入法，使得我不用安装中文输入法也能正常使用。

尤其是，我现在用双拼，而且用 dvorak。我希望它能完全自定义布局和按键响应。

---

# 网页输入法拼音→汉字候选开源库完整对比（适配双拼、浏览器离线、可拆分Rime）

分三大梯队：**轻量纯 JS 零依赖（快速开发）、Wasm 精简 Rime（自定义天花板，你的 Dvorak \+ 双拼首选）、成熟完整 Web 输入法成品（开箱即用）**

## 一、先回答你最关心的：Rime 能不能拆、体积能压到多小

### 1\. librime 拆分与 Web 编译现状

librime 原生依赖 boost、yaml\-cpp、leveldb、lua，完整编译体积很大，但**可以深度裁剪**：

#### 可直接砍掉的模块（大幅瘦身）

- Lua 脚本、emoji、繁简转换 opencc、五笔 / 仓颉等其他方案、英文混输、符号扩展、云同步、历史记录持久化

- 完整 N\-gram 语言模型（只保留单字 \+ 双字词基础词典）

#### Web 可用成品：fcitx5\-rime\.js（社区预编译 Wasm Rime）

GitHub：`Mintimate/fcitx5-rime.js`，专门给浏览器做的精简 Rime Wasm 构建

- 裁剪后 Wasm 主体：**gzip 后～300\~500KB**

- 词库单独分包：基础 8000 常用汉字词典 gzip 仅 30KB，完整常用词库 150KB

- 原生支持**自定义双拼 schema**（小鹤、微软、自制双拼 yaml 方案直接复用本地 Rime 配置）

- 完美分离：键盘布局（Dvorak 映射你前端 JS 自己处理）→ 把转好的标准字母编码丢给 Rime 内核出候选

#### 缺点

1. 初次加载有一点耗时；

2. 编译配置门槛高，不熟悉 C\+\+/Emscripten 很难自己二次裁剪；

3. 整体比纯 JS 库重，但自定义能力无对手（你要完全自制双拼规则只能选它）

### 2\. 自己拆 librime 的可行路径（极简内核）

1. 只保留核心链路：`speller（拼音解析）+ translator（词典匹配）+ schema（双拼规则）`

2. Emscripten 编译时禁用所有第三方依赖，用内置极简内存分配

3. 词库不打包进 Wasm，前端异步加载 yaml 字典文本，内存动态载入

4. wasm\-opt 极致压缩 `-Oz`，可再减 20% 体积

## 二、轻量纯 JS 拼音→汉字库（无 Wasm、零依赖、最小体积，适合简单双拼）

适合：只需要基础双拼单字 / 两字词、不想引入 Wasm、单 HTML 离线运行

### 1\. pinyinjs（最轻量化，首选简易方案）

仓库：sxei/pinyinjs

- 体积：常用汉字字典 26KB \(gzip\)，完整字库 122KB

- 能力：拼音↔汉字互转，内置简易输入法 demo，**可自行封装双拼转换层**

- 适配思路：
前端 JS 写一层双拼映射表（你的自定义双拼声母韵母），把输入序列转成全拼，再丢给 pinyinjs 查候选

- 局限：无智能调频、无整句联想，只有静态字 / 词表，自定义双拼规则需要自己手写映射逻辑

- 优势：打开 HTML 直接跑，无任何构建工具依赖

### 2\. pinyin\-ime（现成网页输入法内核，带完整候选逻辑）

npm：pinyin\-ime

- 纯 TS 实现，自带输入缓冲区、候选面板、翻页、中英文切换

- 原生支持修改拼写解析器，可注入自定义双拼编码转换

- 内置 Lit 自定义元素，Vue/React/ 原生 HTML 通用

- 词库分基础 / 完整版，gzip 最小 40KB，开箱不用自己写候选渲染逻辑

- 适合：不想重复造按键监听、候选弹窗轮子，只改双拼规则

### 3\. pinyinlite（极速匹配，检索性能最强）

- gzip 最小 25KB（8000 常用字），千万字 / 秒检索速度

- 只做「拼音串匹配汉字数组」底层能力，无上层输入交互

- 适合：你自己完整手写输入框架，只需要底层拼音查表模块

### 4\. cnchar /pinyin\-pro（侧重汉字转拼音，反向输入能力弱）

腾讯 pinyin\-pro、cnchar 主要用途是**汉字输出拼音**，反向拼音查汉字仅基础功能，不适合做输入法候选引擎，不推荐作为核心。

## 三、完整 Web 输入法开源成品（直接二次修改，内置双拼支持）

如果你不想从零拼接按键监听 \+ 拼音内核，可以基于这些项目魔改，自带完整交互：

1. **chromeos\-shuangpin\-ime**（Gitee kaiserzh/chromeos\-shuangpin\-ime）
浏览器插件双拼输入法，原生支持多种双拼方案，离线词库，可剥离插件逻辑改成独立网页版；支持自定义编码规则。

2. 各类单 HTML 离线输入法（github 搜 web\-shuangpin）
纯静态 HTML，内置手写双拼码表，可替换 Dvorak 键盘映射层。

## 四、三类方案选型对比（针对你的需求：Dvorak \+ 自定义双拼、网页内运行）

|方案|体积|自定义双拼能力|智能调频 / 联想|上手难度|最佳场景|
|---|---|---|---|---|---|
|精简 Wasm Rime \(fcitx5\-rime\.js\)|300\~500KB\(gzip\)|★★★★★<br>直接导入自制双拼 yaml，零手写映射|完整（词频、整句、容错码）|中高，需处理 Wasm 通信|重度使用、完全自定义双拼、长期打字|
|纯 JS pinyin\-ime|40\~100KB\(gzip\)|★★★<br>需自己写双拼转全拼映射|基础两字词，无高级调频|低|轻量自用、单 HTML 离线打开、简单输入|
|pinyinjs/pinyinlite|25\~120KB\(gzip\)|★★<br>完全手写双拼解析逻辑|仅静态字表|极低|极简 demo、临时使用、不需要联想|

## 五、针对你「Dvorak \+ 自制双拼」的推荐落地路线

### 路线 A（推荐，重度自定义）：fcitx5\-rime\.js 精简 Wasm Rime

1. 前端 JS 层：

    - 监听 keydown，Dvorak 物理键 → 标准 qwerty 字母映射（独立配置，不影响 Rime）

    - 过滤 Ctrl/Alt 系统快捷键，拦截原生输入，缓存编码串

2. Wasm Rime 层：

    - 放入你自制的双拼 schema\.yaml（和本地 Rime 通用，支持自定义声母、韵母、零声母、容错码）

    - 接收 JS 传过来的字母串，返回排序后的候选数组

3. UI 层：自己写跟随光标候选弹窗、数字选字、逗号句号翻页、全角半角切换

4. 瘦身优化：只加载 8000 常用字基础词库，禁用 Lua、emoji、繁简转换

### 路线 B（极简快速，临时自用）：pinyinjs \+ 手写双拼转换

1. 自建两层映射 JS：
① Dvorak 物理键 → 标准字母
② 自定义双拼编码表 → 完整全拼字符串

2. 用 pinyinjs 根据全拼查汉字候选

3. 优点：无编译、无 Wasm，单个 HTML 双击就能打开；缺点：没有智能调频，越打越准的体验缺失

## 六、关键补充：Rime 不用全量，词库与方案分离

Rime 的**输入方案（双拼规则）** 和**词库**是完全分开的 yaml 文件：

- schema：只定义双拼编码逻辑（几十 KB 文本）

- dict：字 / 词汇表（可按需裁剪）
你完全可以只加载自己写的双拼 schema，抛弃所有多余方案，进一步减小加载体积。

## 七、避坑要点

1. 不要直接用完整 librime 桌面版编译 Web，依赖太多，体积会突破 2MB；优先用社区预裁剪的 fcitx5\-rime\.js

2. Dvorak 键盘映射**不要交给 Rime 处理**，Rime 只处理拼音编码，物理布局映射在前端 JS 做，分离更灵活

3. 纯 JS 库如果要自定义非常规双拼（自制声母韵母），需要手动实现音节分割逻辑，工作量比 Rime 大很多

4. 所有方案都支持 LocalStorage 存储用户自造词、自定义布局 / 双拼配置，离线持久化

需要我给你一份极简可运行的 demo 骨架吗？分两种：①纯 JS 双拼 demo（pinyinjs）、②Wasm Rime 前后端通信最简调用示例。

> （注：部分内容可能由 AI 生成）
